include classpath("conf/base.conf")

db.dialect="MYSQL"
db.hikari."dataSourceClassName"="org.h2.jdbcx.JdbcDataSource"
db.hikari."dataSource.url"="jdbc:h2:mem:test;MODE=MYSQL;NON_KEYWORDS=YEAR"
db.hikari."dataSource.user"=test
db.hikari."dataSource.password"=test